# Test environment variables - using same database as development
NODE_ENV=test

# Use the same database as development
# Note: In a real project, you might want to use a separate test database
# and manage test data carefully to avoid conflicts
POSTGRES_USER=user_service
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=user_service
POSTGRES_PORT=5432
POSTGRES_HOST=localhost

# Connection URL for Drizzle
DATABASE_URL=postgres://user_service:your_secure_password_here@localhost:5432/user_service
